<%= form_for(@request_item) do |f| %>
  <% if @request_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_item.errors.count, "error") %> prohibited this request_item from being saved:</h2>

      <ul>
      <% @request_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :requiest_id %><br>
    <%= f.number_field :requiest_id %>
  </div>
  <div class="field">
    <%= f.label :item %><br>
    <%= f.text_field :item %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
